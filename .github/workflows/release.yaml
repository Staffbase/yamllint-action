---
name: Release Drafter

on:
  push:
    branches:
      - main

jobs:
  release:
    uses: Staffbase/gha-workflows/.github/workflows/template_release_drafter.yml@v16.0.0
    secrets:
      client_id: ${{ vars.STAFFBASE_ACTIONS_CLIENT_ID }}
      private_key: ${{ secrets.STAFFBASE_ACTIONS_PRIVATE_KEY }}
